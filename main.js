// Reads the data and creates barebones bars
// Numeric values will be displayed and the position specified by user
const drawBarChart = function (data, options, element) {
  return;
}


// Sets bar colors
const barColor = function (color) {
  return;
}

// Sets label colors
const labelColor = function (color) {
  return;
}

// Sets bar spacing
const barSpacing = function (spacing) {
  return;
}

// Sets x-axis lables
const xAxis = function () {
  return;
}

// Sets y-axis values (make user specify the spacing between ticks in 'options')
const yAxis = function (options) {
  return;
}

// Sets title
const setTitle = function (options) {
  return;
}

